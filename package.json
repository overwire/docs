{
  "name": "overwire-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/build-llms-txt.mjs",
    "preview": "astro preview",
    "test": "node --test",
    "check:schemas": "node scripts/check-schemas.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.40.0",
    "@fontsource/geist-mono": "^5.2.8",
    "@fontsource/geist-sans": "^5.2.5",
    "astro": "^6.4.6",
    "sharp": "^0.35.1"
  },
  "engines": {
    "node": ">=22"
  }
}
