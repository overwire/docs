---
title: overwire run
description: "Execute a workflow locally (mock by default; live requires a runner)."
sidebar:
  order: 9
---

Execute a workflow locally (mock by default; live requires a runner).

```sh
overwire run [options] [file]
```

The full option reference for this page is being written. Run `overwire run --help` for the complete, current option list.
